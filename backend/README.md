# PromoCode Manager API

NestJS backend. See the repository root `README.md` for Docker and run instructions.

```bash
npm install
npm run start:dev
```

Uses `.env` from this folder or parent folder (`../.env`).
